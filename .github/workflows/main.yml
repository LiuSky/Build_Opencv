name: 编译OpenCV-iOS13-精简100MB版

on:
  workflow_dispatch:

jobs:
  build-opencv:
    runs-on: macos-15
    permissions:
      contents: write

    steps:
      - name: 1. 环境检查与指定 Xcode 16.4
        run: |
          sudo xcode-select -s /Applications/Xcode_16.4.app
          xcodebuild -version

      - name: 2. 拉取 OpenCV 源码 (4.12.0)
        uses: actions/checkout@v4
        with:
          repository: opencv/opencv
          ref: 4.12.0
          path: opencv_source
          fetch-depth: 1

      - name: 3. 强力破解脚本并开始编译
        run: |
          mkdir -p output
          # 修改脚本防止 i386 报错
          sed -i '' 's/\["i386", "x86_64"\]/\[\]/g' opencv_source/platforms/ios/build_framework.py
          sed -i '' 's/targets = self.get_default_targets()/targets = [("iphoneos", "arm64")]/g' opencv_source/platforms/ios/build_framework.py
          
          # 策略：去掉不需要的大型模块以缩减体积
          # 保留：core, imgproc, imgcodecs, features2d, objdetect, objc
          # 移除：dnn, stitching(拼接), ml(机器学习), video(视频分析), calib3d(3D校准), gapi, photo
          python3 opencv_source/platforms/ios/build_framework.py output \
            --opencv opencv_source \
            --iphoneos_archs arm64 \
            --iphonesimulator_archs "" \
            --build_only_specified_archs \
            --iphoneos_deployment_target 13.0 \
            --without dnn \
            --without stitching \
            --without ml \
            --without video \
            --without calib3d \
            --without gapi \
            --without photo \
            --without java \
            --without js \
            --without python_bindings_generator
          
      - name: 4. 关键步骤：执行瘦身 (Strip)
        run: |
          # 只有执行了 strip，文件才能从 300MB 降到 100MB 以下
          REAL_BINARY="output/opencv2.framework/Versions/A/opencv2"
          if [ -f "$REAL_BINARY" ]; then
            echo "==== Strip 前大小 ===="
            ls -lh "$REAL_BINARY"
            
            # -S 移除调试符号，保留业务逻辑，支持 OC 调用
            strip -S "$REAL_BINARY"
            
            echo "==== Strip 后大小 ===="
            ls -lh "$REAL_BINARY"
          else
            echo "错误: 找不到二进制文件"
            exit 1
          fi

      - name: 5. 压缩 Framework
        run: |
          cd output
          zip -r ../opencv2_ios13_100mb_objc.zip opencv2.framework

      - name: 6. 上传到 GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v4.12.0-ios13-100mb
          name: OpenCV 4.12.0 iOS 13 (OC版-约100MB)
          files: opencv2_ios13_100mb_objc.zip
          body: |
            ### 优化说明
            - **目标**: 体积控制在 100MB 左右，支持直接 Push 到 Git 仓库。
            - **包含**: Objective-C 包装层、Core、Imgproc、Features2d、Objdetect。
            - **移除**: DNN, Stitching, ML, Video, Calib3d, Photo 等。
            - **瘦身**: 已执行 `strip -S`，剔除调试符号。
            - **架构**: arm64 (iOS 13.0+)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
